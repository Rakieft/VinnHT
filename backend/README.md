# VinnHT Backend

1. Copier `.env.example` vers `.env` et adapter les identifiants MySQL.
2. Lancer `npm install`.
3. Créer les tables avec `npm run db:migrate`.
4. Démarrer l’API avec `npm run dev`.

L’API démarre par défaut sur `http://localhost:5056`.
